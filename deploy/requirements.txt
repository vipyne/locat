# Cloud image deps — a Linux-safe SUBSET of the repo's deps.
#
# Deliberately NOT the repo's pyproject extras: those include `mlx-whisper` and `local`
# (PyAudio), which are macOS / Apple-Silicon only and will FAIL to install on the Linux
# ARM64 base image. This is why the cloud variant has its own dependency file.
#
#   daily   -> daily-python (the cloud WebRTC transport)
#   whisper -> faster-whisper (cross-platform STT, replaces WhisperSTTServiceMLX)
#   kokoro  -> kokoro-onnx (TTS; same as local, cross-platform)
#   silero  -> Silero VAD (bundled/onnx; Smart Turn v3 also ships in the base package)
#
pipecat-ai[daily,kokoro,silero,whisper]>=1.6.0
num2words>=0.5.14
