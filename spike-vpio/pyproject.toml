[project]
name = "spike-vpio"
version = "0.1.0"
description = "Spike: macOS Voice Processing I/O (VPIO) echo cancellation prototypes — see ../SPIKE_VPIO.md"
requires-python = ">=3.12"
# Deps intentionally empty until Phase 1 chooses an approach
# (candidates: pyobjc-framework-AVFoundation, pyobjc-framework-CoreAudio, numpy).
dependencies = []
