=== ralph iteration 3/25 (20260721-144105) ===
Iteration complete. 

**What I did this iteration:**
- Picked the first incomplete task: Phase 0's `.gitignore`.
- Created `.gitignore` ignoring `models/`, `.venv/`, `.env`, `__pycache__/`, and `*.pyc`.
- **Verified** with `git check-ignore` — all six representative paths (including nested `models/ollama/blob` and `__pycache__/foo.pyc`) match correctly.
- Checked the task off in `PROGRESS.md` with a note, and committed (`d7dd95d`).

**Phase 0 is now fully complete.** Per the one-task-per-iteration rule, I'm stopping here. The next iteration's first incomplete task is **Phase 1 — Dependencies**: adding Pipecat with local-service extras (whisper-mlx, kokoro, ollama, silero, local-smart-turn + pyaudio, python-dotenv, loguru, textual), which requires confirming the exact extra names via the `pipecat-context-hub` MCP server before running `uv add`/`uv sync`.
