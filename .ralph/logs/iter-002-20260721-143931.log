=== ralph iteration 2/25 (20260721-143931) ===
Iteration complete. One task done, verified, committed.

## Summary

**Task done this iteration:** Phase 0 — Scaffold & Python env (first task).

- Ran `uv init --bare --python 3.12 --name locat` + `uv python pin 3.12` → created `pyproject.toml` and `.python-version` (3.12).
- Tightened `requires-python` to `>=3.12,<3.13` so uv can't resolve 3.14 wheels (the plan explicitly rules out 3.14 for ML wheels; the machine's default Python is 3.14).
- **Verify passed:** `uv run python -c "import sys; print(sys.version)"` → `3.12.0`. uv created `.venv` (auto-ignored via its own `.venv/.gitignore`).
- Updated `PROGRESS.md` (checked off the `uv init` task + its verify, added a notes-log entry) and committed as `c2c72a5`.

**Next iteration picks up:** Phase 0's remaining task — create the tracked `.gitignore` (`models/`, `.venv/`, `.env`, `__pycache__/`, `*.pyc`) — then Phase 1 dependencies (which will need the `pipecat-context-hub` MCP to confirm exact extra names, per the plan's "confirm at implementation" list).
