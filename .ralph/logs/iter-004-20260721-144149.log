=== ralph iteration 4/25 (20260721-144149) ===
This iteration is complete. **Phase 1 — Dependencies** is done and verified.

**What I did this iteration:**
- Confirmed exact Pipecat extra names via the `pipecat-context-hub` (not guessing): `mlx-whisper`, `kokoro`, `ollama`, `silero`.
- **Key finding:** Smart Turn v3 is now **bundled in core** pipecat-ai — the plan's guessed `local-smart-turn` extra does not exist.
- **Surprise fixed this iteration:** `pipecat.services.whisper.stt` imports `faster_whisper` at module load (CPU + MLX classes live in one module), so the `whisper` extra is required *alongside* `mlx-whisper` or every import fails. Added it.
- Final deps: `pipecat-ai[kokoro,mlx-whisper,ollama,silero,whisper]>=1.5.0` + `pyaudio`, `python-dotenv`, `textual`, `loguru`. PortAudio 19.7.0 was already brew-installed.
- **Verified:** `uv sync` clean (123 packages, Pipecat 1.5.0, Python 3.12.0); all 9 service/pipeline classes import cleanly; `check_deprecation` on the three non-standard imports → none deprecated.
- Updated `PROGRESS.md` and committed (`f8397ce`).

**Next iteration:** Phase 2 — model acquisition (`scripts/run_ollama.sh` + `scripts/prefetch_models.py`). Note flagged in PROGRESS.md: that phase's *verification* (pulling qwen2.5:14b ~9GB + HF caches) needs network — if network is unavailable when reached, it's a legitimate `RALPH_BLOCKED` (writing the scripts is still doable headless; only their model-download verify is network-gated).
