=== ralph iteration 9/25 (20260721-150101) ===
Iteration complete. One task done, verified, committed.

**What I did this iteration:** Phase 3 STT sub-task — added `build_stt()` to `bot.py`.

- **Confirmed the API via the context hub (not guessed):** the current constructor is `WhisperSTTServiceMLX(settings=WhisperSTTServiceMLX.Settings(model=...))`; the bare `model=` kwarg is deprecated as of Pipecat 0.0.105, so I used the `Settings` form. `MLXModel` members are `TINY, MEDIUM, LARGE_V3, LARGE_V3_TURBO`.
- **Default:** `LARGE_V3_TURBO` (`mlx-community/whisper-large-v3-turbo`, already prefetched in Phase 2), selected by `WHISPER_MODEL` env matched to the enum member name via `MLXModel[name]` — same convention as `scripts/prefetch_models.py`, so prefetch and bot stay in sync.
- **Kept the import-side-effect-free design:** construction is cheap and offline (MLX loads weights lazily on first transcribe).
- **Verified:** with `HF_HUB_OFFLINE=1`, `import bot` + `build_stt()` returns a real `WhisperSTTServiceMLX` with zero network.

**Next iteration:** Phase 3 LLM sub-task — `OLLamaLLMService(model=<env>, base_url=<env>)` (confirm constructor args via context hub first).
